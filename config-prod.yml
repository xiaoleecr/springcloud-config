spring:
  application:
    name: cloud-payment-service
  datasource:
      type: com.alibaba.druid.pool.DruidDataSource          #当前数据源操作类型
     # driver-class-name: org.gjt.mm.mysql.Driver
      driver-class-name: com.mysql.cj.jdbc.Driver     #数据包驱动
      url: jdbc:mysql://localhost:3306/db2019?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC            #数据库名称
      username: root
      password: root

# eureka:
#   client:
#     fetch-registry: true
#     register-with-eureka: true #加入服务注册
#     service-url:
#       defaultZone: http://eureka7002.com:7002/eureka/,http://eureka7001.com:7001/eureka/
#   instance:
#     instance-id: payment8001
#     prefer-ip-address: true #eureka主页列表中显示具体ip地址和端口


mybatis:
  #config-location: classpath:mybatis/mybatis.cfg.xml  #mybatis配置文件所在路径
  type-aliases-package: com.atguigu.springcloud.entities  #所有entity别名类所在包
  mapper-locations: classpath:mapper/*.xml  #mapper映射文件

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true

info:
  app:
    name: "hello world"